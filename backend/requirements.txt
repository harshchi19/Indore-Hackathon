# Verdant Backend – Part A Dependencies

# Framework
fastapi==0.115.6
uvicorn[standard]==0.34.0
pydantic[email]==2.10.4
pydantic-settings==2.7.1

# MongoDB / ODM
motor==3.6.0
beanie==1.27.0
dnspython==2.7.0

# Redis (caching / future pub-sub)
redis[hiredis]==5.2.1

# Security
PyJWT==2.10.1
argon2-cffi==23.1.0

# Task queue (Part B will extend with Celery workers)
# celery==5.4.0  # uncomment when needed

# WebSockets (explicit for Render / Docker compatibility)
websockets>=12.0

# Utilities
python-dotenv==1.0.1
certifi>=2024.0.0        # CA bundle for MongoDB Atlas TLS on Render

# Graph Database
neo4j>=5.0,<6.0        # Neo4j async driver for graph-based recommendations

# Dev / Test
httpx==0.28.1
pytest==8.3.4
pytest-asyncio==0.25.0
